import type { Metadata } from "next";
import "./globals.css";
import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Abhinav Karna – Portfolio",
  description:
    "Portfolio of Abhinav Karna – web developer, cloud & cybersecurity enthusiast, USA alumni, EIMUN delegate, and digital marketer for MG Steel & Rukmani.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} bg-black text-white`}>
        {children}
      </body>
    </html>
  );
}
